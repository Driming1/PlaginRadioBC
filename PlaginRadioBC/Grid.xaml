<Window x:Class="PlaginRadioBC.Grid"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlaginRadioBC"
        xmlns:helper="clr-namespace:PlaginRadioBC.Helpers.CustomStyles"
        mc:Ignorable="d"
        Title="SAMPLING RESULT"  d:DesignHeight="500" d:DesignWidth="1189" Height="500" Width="900" ResizeMode="NoResize">
    <!--Style="{StaticResource CustomWindowTitleStyle}"   Стиль для тайтла (Не закончен) Нужно менять поведение всего (Кнопка клоуз, хват и перенос и тд)-->

    <Window.DataContext>
        <local:MainWindowViewModel />
    </Window.DataContext>
    
    <Window.Resources>
        
        
        <!--Стили для Грида-->
        <Style x:Key="DataGridColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        
        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="5"/>
        </Style>


    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>

                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" VerticalAlignment="Center" Grid.Column="0" Text="Nova Sloboda" FontWeight="Bold" FontSize="24" Margin="20,10,0,0"/>
                <StackPanel Style="{StaticResource CustomStackPanelStyle}"  Grid.Row="0" Grid.Column="1">
                    <helper:TitleValueControl Title="Заголовок" Value="Значение"/>
                </StackPanel>
                <StackPanel Style="{StaticResource CustomStackPanelStyle}" Grid.Row="0" Grid.Column="2">
                    <helper:TitleValueControl Title="Заголовок" Value="Значение" />
                </StackPanel>
                <StackPanel Style="{StaticResource CustomStackPanelStyle}" Grid.Row="0" Grid.Column="3">
                    <helper:TitleValueControl Title="Заголовок" Value="Значение" />
                </StackPanel>
                <StackPanel Style="{StaticResource CustomStackPanelStyle}" Grid.Row="0" Grid.Column="4">
                    <helper:TitleValueControl Title="Заголовок" Value="Значение" />
                </StackPanel>
            </Grid>
        </StackPanel>
            
        <DataGrid Margin="0,15,0,0" Grid.Row="1" x:Name="CustomDataGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" ItemsSource="{Binding Data}" 
                  CellStyle="{StaticResource DataGridCellStyle}" ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
            <DataGrid.Resources>
                <DataTemplate x:Key="SelectAllHeaderTemplate">
                    <CheckBox IsChecked="{Binding IsAllSelected, RelativeSource={RelativeSource AncestorType=DataGrid}, Mode=TwoWay}" 
                  Command="{Binding DataContext.ToggleAllCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                </DataTemplate>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Select" Binding="{Binding IsChecked}" Width="50" IsReadOnly="False"
                        HeaderTemplate="{StaticResource SelectAllHeaderTemplate}" />
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="Auto" />
                <DataGridTextColumn Header="TABLE" Binding="{Binding Table}" Width="Auto" />
                <DataGridTextColumn Header="NAME" Binding="{Binding Name}" Width="Auto" />
                <DataGridTextColumn Header="FREQ, MHz" Binding="{Binding FreqMHz}" Width="Auto" />
                <DataGridTextColumn Header="CH" Binding="{Binding CH}" Width="Auto" />
                <DataGridTextColumn Header="DIST, km" Binding="{Binding DistKm}" Width="Auto" />
                <DataGridTextColumn Header="EIR dBW" Binding="{Binding EIRdBW}" Width="Auto" />
                <DataGridTextColumn Header="HEFF, m" Binding="{Binding HeffM}" Width="Auto" />
                <DataGridTextColumn Header="STATE" Binding="{Binding State}" Width="Auto" />
                <DataGridTextColumn Header="SYS." Binding="{Binding Sys}" Width="Auto" />
                <DataGridTextColumn Header="INTEREF, dBmkV/m" Binding="{Binding Interference}" Width="Auto" />
                <DataGridTextColumn Header="VICTIM, dBmkV/m" Binding="{Binding Victim}" Width="Auto" />
                <DataGridTextColumn Header="LON" Binding="{Binding Lon}" Width="Auto" />
                <DataGridTextColumn Header="LAT" Binding="{Binding Lat}" Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2"  HorizontalAlignment="Left">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="140"/>

                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Height="40" Content="Upload HTZ" Foreground="#4A5A6B"
                Margin="5,0,5,0" 
                Background="#F5F5F5" 
                BorderBrush="#B0B0B0" 
                BorderThickness="0.1"
               >
                </Button>
                <Button Grid.Column="1" Height="40" Content="Calc sampling" Foreground="#4A5A6B"
                Margin="5,0,5,0" 
                Background="#F5F5F5" 
                BorderBrush="#B0B0B0" 
                BorderThickness="0.1"
                >
                </Button>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
